<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Whowhatwhy" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Whowhatwhy" %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  
  </head>
  <body>
    <header>
     <div class='container'>
      <div class='row'>
        <%= render 'layouts/navigation' %>
        </div>
      </div>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <div class='container'>
        <div class='row'>
           <%= yield %>
         </div>
       </div>
    </main>
  </body>
</html>
